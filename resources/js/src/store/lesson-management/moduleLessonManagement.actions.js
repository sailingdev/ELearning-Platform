import axios from '../../http/axios'
export default {
    storeLanguage({commit}, payload){
        console.log(payload)
        // return new Promise((resolve, reject)=> {
        //
        // })
    }
}
